[package]
name = "protocol-types"
version = "0.0.0"
edition = "2021"
description = "Shared wire types for Sona. No crypto, no I/O — just the contract between client and server."
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
hex = { workspace = true }
